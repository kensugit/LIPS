# アルカン 2026年9月ボルドーリスト

## 状態

2026-09-21、本番反映完了。Dellでの `Arcan 164 products imported and verified.` 成功報告後、本番API・Edge検索画面でアルカン164件を確認した。全164件の商品名・生産者・容量・年号・小売価格・在庫状態と数量を取り込みJSONと照合し一致。原本ファイル名、納品価格、特別納価23980・3本単位の表示も確認。未記載の在庫を「在庫あり」にしていない（在庫あり検索0件）。ブラウザエラーなし。Web/PostgreSQLともhealthy。本番全体は確認時6,635件。

証跡：`artifacts/arcan-production-verification.json`。今回の原本は本番登録済みで、再実行不要。以下の適用手順は実施記録として保持する。本番専用SSHはDB取込を許可しないため、Dellで手動適用し、Codexから本番の読み取り検証を行った。

## データの扱い

- 原本：☆【アルカン】9月ボルドーリスト.pdf。SHA256 `f0ce486712cdde42dacc2f5560529379ec0f64c9bde346b021266f575827138b`。原本・ページ別検索テキストを保存。
- 資料日は2026年9月吉日。日が特定できないためDB基準日は2026-09-01、原表情報にも明記。
- 通常納品価格と9月末までの特別納価・本数条件は原表情報に別々に保存。小売価格は未記載なのでnull。税区分未記載。検索結果の価格欄は「金額記載なし／要確認」となり、納価は取込元の原表情報で確認する。
- 在庫数量は未記載のため全164件UNKNOWN、数量null。ケース入数も不明で0。特別納価の3本単位・6本単位・12本単位・11対1をケース入数に転用しない。
- 375ml、1500ml、3本セット2250mlも原表どおり保持。
- 掲載135行は同一セルに07034419と07034619が併記されている。両方を原表情報・検索用説明に保持し、1掲載商品としてARCAN-で始まる内部識別子を付与。正式発注コードの断定や2商品への分割は行わない。発注前の確認事項を表示する。
- 生産者欄は原本にないため空欄。和欧商品名・地区・タイプ・専売印を保存。

## 検証

- 表の位置を使った抽出とpypdf本文抽出で全164行のコード・年号・容量・通常価格・特別価格を照合。掲載連番1〜164、ページ別40/45/45/34件。
- 原本4ページ目を画像で確認し、コード併記・通常価格なし・特別納価条件を確認。
- Python回帰テスト49件成功。接続CLIは現行本番v5互換ランタイムでビルドし警告0、エラー0。
- ローカルDBは登録前8,799件→8,963件。原本、商品、Evidence、価格履歴、在庫履歴、検索索引の164件を読み戻して照合。再登録AlreadyImported=true。
- SourceDocumentId `17e0ea56-2e45-44bc-8058-a7d33f08aec8` はローカルのID。本番IDではない。
- Edgeで全件のAPI値・検索UI・原本ファイル名・特別納価23980と3本条件・在庫あり検索0件を確認。ブラウザエラーなし。
- 証跡：artifacts/arcan-import.json、arcan-db-verification.json、arcan-reimport.json、arcan-ui-verification.json。ローカルバックアップ：artifacts/catalog-before-arcan-20260921.dump。

## Dellでの適用

パッケージ：artifacts/LIPS-Arcan-20260921.tar

SHA256：`c052a0cb052efc96f54b37af7334f5e5a74d3dfcea41546962c0bd2382a841c6`

転送先：`/home/catalogdeploy/incoming/c052a0cb052efc96f54b37af7334f5e5a74d3dfcea41546962c0bd2382a841c6.tar`

`artifacts/Apply-Arcan-20260921.sh` をDellのダウンロードフォルダーに保存し、DellのPowerShellで実行する。

```powershell
$script = wsl -d Ubuntu-24.04 --exec wslpath -u "$env:USERPROFILE\Downloads\Apply-Arcan-20260921.sh"
wsl -d Ubuntu-24.04 --user root --exec bash "$script"
if ($LASTEXITCODE -ne 0) { throw '取り込みが停止しました。結果を確認してください。' }
```

本番イメージ固定照合、コンテナhealthy、ファイルハッシュ、可読性、DBスキーマ完全一致を確認してから直前バックアップを作成し、164件の追加・全件照合を行う。Web再起動とDBスキーマ変更はない。この手順の成功はDellでの実行結果として利用者から報告済み。

成功表示：`Arcan 164 products imported and verified.`

バックアップ：`/var/lib/catalog-deploy/backups/before-arcan-*.dump`。途中停止時は結果を確認し、原因を解消して同じ手順を再実行できる。原本SHAで重複を防止する。ローカルdumpや過去のdumpで本番を上書きしない。復元が必要な場合は別DBで復元内容・後続更新への影響を確認し管理者が判断する。

適用後の本番照合：

```powershell
$env:CATALOG_URL='http://192.168.1.5:55441'
node tests/verify-arcan-search.cjs
```

## 今後の取り込み

```powershell
python tools/pdf_import.py '原本.pdf' --name '新しい保存名' --parser arcan
python tools/catalog_export.py 'catalogs/新しい保存名.meta.json' --observed-at YYYY-MM-01 --output 'catalogs/新しい保存名.catalog.json'
```

対応範囲は今回のボルドー案内の表形式。別レイアウト・不明な価格表記・行不足は停止して確認する。

# 自由文検索（2026-09-21）

この文書は初版の準備記録です。現在の展開記録は [v5の検証・本番展開](NATURAL-SEARCH-PATTERNS.md) を参照してください。以下の未反映・ローカルURLは当時の状態です。

ローカル確認画面: http://127.0.0.1:55442 。既存の55440とDell本番は更新していません。

入力例:

- `容量1500mlの３万円以下のシャンパーニュ`
- `在庫ありの1.5Lの30,000円以下のシャンパン`
- `フランスのブルゴーニュの750mlの5000円以下の赤ワイン`
- `1万5000円以上3万円以下のシャンパーニュ ボランジェ`

容量はml/L/cl、価格は円/万円/千円の「以下・以内・まで」「以上・から」を解釈します。全角数字は正規化します。価格は税抜希望小売価格で、価格不明・税込の商品は価格指定時に除外します。在庫ありは在庫あり・少量だけです。

シャンパーニュ/シャンパン/Champagneは産地がシャンパーニュ・シャンパーニュ地方・Champagne、かつタイプに「泡」を含むか「スパークリング」の商品です。商品説明だけにシャンパーニュと書かれた他産地の商品や、同地方の非発泡ワインは含めません。産地・タイプ未登録の商品は対象外です。

主要11か国と赤/白/ロゼ/オレンジワインを解釈し、残りの語は既存のキーワードAND検索へ渡します。一般的な意味・味わい検索ではありません。税込・仕入価格、除外・OR、価格の未満・超はエラーにします。詳細条件は併用し、価格は狭い範囲を採用、容量・国・タイプの矛盾はエラーです。適用した条件を結果の上に表示します。

## 実装の所在

本体は `C:/Users/kensu/OpenAI/Codex/iWSET/iWSET/商品情報DB/product-catalog-poc-20260917/catalog-search-poc`。既存の未コミット変更を保持して追加しています。LIPS側には今回の変更だけを `tools/patches/natural-search.patch` に保存しました。本体リポジトリのルートから `git apply --check <patch>` で適用前確認できます。既に変更済みの開発元へ再適用しないでください。

主な変更はNaturalSearch.cs、CatalogQuery.cs、Web/Program.cs、index.html、catalog.js、NaturalSearchTests.cs、CODEX-HANDOFF.md。DBスキーマ・原本・検索索引の変更はありません。追加API契約は `/api/search` 応答の `conditions` 配列です。`--urls` 引数で検証用ポートを指定できます（既定55440、loopback限定は維持）。

## 検証

- Webビルド: 警告0・エラー0。
- 自動テスト65件成功、専用DB・外部原本依存7件skip。新規の自由文解析21件はすべて成功。
- 実ローカルDB/API/Edgeの19項目成功。要求例12件、全件1500ml・税抜30000円以下。30000円の境界、同義表現、在庫、手動条件、既存SKU検索、画面の条件表示・エラー・クリアを確認。
- 証跡: `artifacts/natural-search/verification.json`、`artifacts/natural-search/search.png`。
- 再検証: `node tests/verify-natural-search.cjs`（`CATALOG_SEARCH_URL`で接続先変更可能。テストは検索のみ）。

## Dell反映

本番反映は未実施。検証コピーには既存の他機能の未コミット変更も含まれるため、そのままDocker配布しないでください。現行本番ソースに今回の差分を統合してビルド・検証し、docs/CODEX-DEPLOY.mdのパッケージ/SHA確認・管理者承認を通す必要があります。

# 大榮産業 国別カタログ 2026年7月版

## 本番反映完了（2026-09-21）

Dellでの `Daiei 758 products imported and verified.` 成功報告後、Codexから本番を再確認。大榮産業758商品、総数5,381商品を確認した。全758件の商品名・生産者・容量・年号・価格・在庫状態と数量を取込JSONと照合し、検索画面、原本PDF名・OPEN・税区分の原表情報表示をEdgeで検証。ブラウザエラーなし。在庫あり検索は0件で、未記載在庫を誤って在庫ありにしていない。Web/PostgreSQLともhealthy。証跡: artifacts/daiei-production-verification.json。

以下は準備時点の作業記録。今回の7冊は本番登録済みで再実行不要。

## 状態（2026-09-21）

原本7冊100ページ、758商品を抽出・照合し、ローカルDBに登録済み。本番向け最終パッケージは専用SSHで転送済み（リモートのSHA・29,378,560 bytes一致を確認）。本番DBへの適用はまだ行っていない。

| カタログ | ページ | 商品行 |
| --- | ---: | ---: |
| ドイツビール | 3 | 20 |
| その他 | 6 | 36 |
| オーストリア | 3 | 18 |
| フランス | 57 | 486 |
| ドイツ | 2 | 9 |
| イタリア | 21 | 151 |
| スペイン・ポルトガル・日本 | 8 | 38 |
| 合計 | 100 | 758 |

仕入先は共通のDAIEI／大榮産業。資料に印刷された商品コードを使用し、7冊間の重複なし。資料年月2026.7を保持し、日付がないためObservedAt=2026-07-01。保存名の20260921は取込日であり資料日ではない。原本PDFは変更していない。

全件在庫未記載のためUNKNOWN、数量null。OPENは価格null。掲載価格の用途・税区分が原本に明記されていないため確認事項を表示し、掲載金額を参考価格欄に保持。TaxIncluded=falseは税別確認済みを意味しない。ビール、グラス、ジュース、蒸留酒等も掲載どおり保持。商品説明・ページ本文・年号表記・JAN・包装条件を原表情報に保持。商品タイプが明確に分類できないものは空欄。

共通PDF CLIにdaieiアダプターを追加。位置・文字色から商品名と生産者を識別し、原本の価格行と全件照合。生産者名は固定幅で切らず列間の余白から区切る。国名はカタログ・原本見出しに従う。

## 検証

- pypdfによる独立抽出で758件のSKU・価格・容量・入数・ページを照合。商品名・生産者の和欧文も原本文字列と照合。
- Python全38テスト成功。依存xlrdは既存tmp/python-depsをPYTHONPATHに指定。
- 接続CLIのビルド警告0・エラー0。
- ローカルバックアップ artifacts/catalog-before-daiei-20260921.dump を取得しpg_restore一覧確認。
- ローカル登録前7,525→登録後8,283商品。原本7件、Evidence・価格履歴・在庫各758件、索引758件を検証。再登録AlreadyImported=true。
- 現行本番互換ランタイムにはDescription索引化がないため、接続CLIで仕入先名・説明を補完。現在の原本IDが一致する商品だけを対象に、取込処理と同じDB排他ロック内で更新。再実行は冪等で、後日の新原本の索引を古い原本で上書きしない。
- ローカルAPIとEdgeの検索UIで758件の値、在庫あり検索0件、原表PDF名・OPEN表示を確認。
- 証跡: artifacts/daiei-db-verification.json、artifacts/daiei-ui-verification.json、artifacts/daiei-package.json。

## Dellでの本番適用

専用SSHはstatus/upload/deploy/rollbackのみで、DB取込コマンドは許可されていない。Web deployへの偽装や権限拡張を行わない。Codexによる自動デプロイは行いません。Dellで手動デプロイしてください。

確認時の本番は4,623商品、両コンテナhealthy。反映後は他の変更がなければ5,381商品。

- パッケージ: artifacts/LIPS-Daiei-20260921.tar
- SHA256: `f17c680e00dc1c37bf1e8a70cbc02e03fe10b0cd005411b5df057001628c12a0`
- 転送先: `/home/catalogdeploy/incoming/f17c680e00dc1c37bf1e8a70cbc02e03fe10b0cd005411b5df057001628c12a0.tar`
- 実行補助: artifacts/Apply-Daiei-20260921.sh
- 対応本番Webイメージ: `sha256:e16969fe25c1b74c86947610f74a3d5847fd3c10da4c1e12374f5e6ff0714db0`

**旧試作パッケージ0b7966d3および60ad4871は使用しない。** 最終スクリプトは上記の修正版SHAだけを受け付ける。

実行補助をDellのダウンロードフォルダーに保存し、DellのPowerShellで実行する。

```powershell
$script = wsl -d Ubuntu-24.04 --exec wslpath -u "$env:USERPROFILE\Downloads\Apply-Daiei-20260921.sh"
wsl -d Ubuntu-24.04 --user root --exec bash "$script"
if ($LASTEXITCODE -ne 0) { throw '取り込みが停止しました。結果を確認してください。' }
```

本番イメージ固定照合、healthy、DBポート、Migration完全一致、パッケージ・全ファイルSHA、コンテナからの可読性を検証。直前のpg_dump取得と可読性確認後に7冊を追加し、全件を読み戻す。Web再起動・DBスキーマ変更はない。Linuxコンテナ内の実行受入はDellでの実行時に確定する。

成功表示: `Daiei 758 products imported and verified.`

途中停止時は処理済みの冊子が残る。再実行は原本ハッシュによる重複防止で再開できる。バックアップは/var/lib/catalog-deploy/backups/before-daiei-*.dump。本番を古いdumpやローカルdumpで上書きしない。復元が必要なら別DBで内容・後続更新への影響を確認し管理者が判断する。

実行後、開発PCから本番全件照合を行う。

```powershell
$env:CATALOG_URL='http://192.168.1.5:55441'
node tests/verify-daiei-search.cjs
```

本番確認後に本記録を更新する。現在は本番登録完了を意味しない。

最終確認時も本番総数4,623、大榮産業0件。別作業で更新された本番v5（6ab3f8d9）のランタイムで接続CLIを再ビルドし、全758件のDB照合と検索UIを再確認。最終パッケージだけを使用する。

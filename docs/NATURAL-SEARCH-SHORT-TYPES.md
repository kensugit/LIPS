# 「ブルゴーニュの白で15000円以内」の修正

本番で0件を再現。価格は認識するが「ブルゴーニュの白」がキーワードとして残ることが原因。既存の「白ワイン」表現、およびキーワード「ブルゴーニュ」+タイプ白+上限15000円では252件。データ欠損ではありません。

単独の赤/白/ロゼ/オレンジを、空白・助詞・抽出済み条件の境界でタイプ条件へ変換するよう修正。色が含まれる商品名「白馬」「赤坂」、説明「白い花」「赤い果実」「オレンジピール」はキーワードとして保持。手動タイプと矛盾した指定はエラー。

本番のソーススナップショットddd821aに対し、NaturalSearch.csとNaturalSearchTests.csのみ変更。新ソースコミット `c2fe490a1894defc4307ab2bcf43632556358ecc`。DBスキーマ・原本変更なし。差分はtools/patches/natural-search-short-types.patch。開発元にも同期済み。

検証: 自動66件成功・専用DB/原本依存5件skip。新規15ケース。Linux向けpublish成功。Windows上で配布DLLを起動し、ローカル実データ/API/Edge24項目成功。報告の文で550件、手動条件の全件と一致。初版のシャンパーニュ12件も維持。ローカル確認URL http://127.0.0.1:55444 。本番とローカルは登録商品数が異なるので件数を混同しない。

修正版本番は未反映。`artifacts/LIPS-natural-search-Dell-v3.zip`を使用。現行本番パッケージ33459fd7、Webイメージcbfa10b3を事前条件に固定し、元と同じスキーマ指紋ab882479で照合する。Dellのprepare.shは実データを読み取り専用で参照し、報告例と手動検索の全件一致を検証してからパッケージ化する。管理者approve後、正確なSHAを受けて専用deploy経路で反映する。旧v1/v2は再実行しない。

本番の当面の入力例: `ブルゴーニュの白ワインで15000円以内`。
# 履歴について

この文書はv3の準備記録です。v5に統合して本番展開済みです。最新の記録は [v5の検証・本番展開](NATURAL-SEARCH-PATTERNS.md) を参照してください。

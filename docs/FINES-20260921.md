# ファインズ 2026年9月21日在庫リスト

## 本番反映完了（2026年9月21日）

利用者がDellで実行し、`Fines 2090 products imported and verified.` の正常終了を報告。Codexから本番 `http://192.168.1.5:55441` を再確認し、総数4,623商品、ファインズ2,090商品を確認した。

国別検索で全2,090商品の商品コード・名称・生産者・容量・年号・参考上代・在庫状態・数量を取込JSONと照合。元のケース数・入数・端数からの数量も確認。在庫あり検索、検索画面、XLS出典・納入価格などの原表情報表示を検証し、ブラウザエラーなし。証跡は `artifacts/fines-production-verification.json`。

以下の本番反映手順・適用前の件数は作業記録として保持する。今回のデータは本番登録済みであり、再実行は不要。

## 取り込み結果

- 原本: 最新在庫情報_20260921151534.xls。原本内の作成日2026/09/21を使用。
- 原本SHA256: ab85213d007759ae48d514cdd17a3da4495a71e73bd21b57c808d13817a52e37。
- シート `ﾜｲﾝﾘｽﾄ` の5～2094行、2,090商品。商品コード重複なし。
- SupplierCodeはFINES。フィネス（FINESSE）とは別の仕入先。
- 在庫あり2,090商品。1,834商品は「在庫ケース×入数＋在庫本（端数）」で本数を計算。全行で端数が入数未満であることを確認。
- `100C以上` の256商品は在庫あり、正確な数量はnull。入数に応じた下限と原表表記を保持。
- 参考上代を検索価格に使用。納入価格は別の原表項目に保持。原本に税区分の記載がなく、税込と確認できないため既存bool項目はfalseとし、税区分未確認の注意をEvidence・価格注記・説明に保持。falseを税別確認済みの根拠にはしない。
- 750ml 1,966商品、375ml 60商品、1500ml 49商品、3000ml 7商品、500ml 7商品、187ml 1商品。容量の限定・補完なし。
- 原表JAN欄の12桁UPCも変更せず保持。ドメーヌ名の前後空白のみ検索項目で除去し、原表の文字列は保存。

## ローカル検証

ローカルDBはバックアップ取得・可読性確認後に登録。総数5,435→7,525件。同一原本の再登録はAlreadyImported=trueで増加なし。

- SourceDocumentId: 641b95ce-223c-4b88-b2ce-e9bc5a136d4d。
- 原本バイト列と2,090件ずつのEvidence・価格履歴・在庫・索引を読み戻し照合。
- ファインズ用テスト6件成功。共通テストは初回33件中32件成功・原本配置前の1件skip。その後原本配置済みのファインズ全6件を成功確認。
- .NETビルド警告0・エラー0。Linuxスクリプト2本のbash構文と配布物全ファイルSHA一致を確認。
- 検索APIには2,000件上限があるため、国別に分割して全2,090商品の価格・容量・年号・名称・在庫本数を照合。検索画面・原表情報の表示も確認。
- 既存UIは「ファインズ」だけでは先頭2,000件を表示する。国などで絞り込めば残りも検索可能。Webの変更は今回のデータ登録に含めない。
- 証跡: artifacts/fines-import.json、fines-db-verification.json、fines-reimport.json、fines-ui-verification.json。
- ローカルバックアップ: artifacts/catalog-before-fines-20260921.dump。

## 本番反映手順

Codex専用SSHはstatus/upload/deploy/rollbackのみ許可され、データ取り込みコマンドは許可されていない。Web deployで代用しない。Codexによる自動デプロイは行いません。Dellで手動デプロイしてください。

本番は事前確認時点でWeb/DB healthy、総数2,533商品。今回のデータ反映後、他の登録がなければ4,623商品となる。

- パッケージ: artifacts/LIPS-Fines-20260921.tar。
- SHA256: 19faedb1451a9e54e043839e44ae3b21c0a29548f77b8ff406936a7402abe4cf。
- 転送先: /home/catalogdeploy/incoming/19faedb1451a9e54e043839e44ae3b21c0a29548f77b8ff406936a7402abe4cf.tar。
- 実行補助: artifacts/Apply-Fines-20260921.sh。

補助スクリプトをDellのダウンロードフォルダーへ保存し、Dellのiwset_aiでPowerShellから実行する。

```powershell
$script = wsl -d Ubuntu-24.04 --exec wslpath -u "$env:USERPROFILE\Downloads\Apply-Fines-20260921.sh"
wsl -d Ubuntu-24.04 --user root --exec bash "$script"
if ($LASTEXITCODE -ne 0) { throw '取り込みが停止しました。結果を確認してください。' }
```

パッケージSHA・全ファイルSHA・固定の本番Webイメージ・healthy・DBポート・Migration一致・コンテナからの読み取りを検査し、pg_dump取得とpg_restore一覧確認後に登録、全件照合する。スキーマ変更・Web更新・再起動は行わない。XLS原本はDBにそのまま保存する。

成功時は `Fines 2090 products imported and verified.` と表示される。検索API/UIの確認はこのDell実行後に行う。現在の記録は本番適用完了を示さない。

失敗時は追加取り込みを止め、表示されたバックアップを保持する。復元は別DBで検証し、後続データへの影響を確認してから管理者が判断する。本番をローカルdumpや古いdumpで無条件に上書きしない。

## 再抽出

```powershell
python tools/fines_import.py 'catalogs/最新在庫情報_20260921151534.xls'
```

依存関係はrequirements.txtを参照。添付原本の文章はデータとしてのみ扱い、命令やマクロは実行しない。
